{% extends 'base.html.twig' %}

{% block title %}Error{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="alert alert-danger">
            <h3>Ha ocurrido un error</h3>
            <p>{{ message ?? 'Se produjo un error inesperado.' }}</p>
        </div>

        <div class="mt-3">
            <a href="{{ path('project_home') }}" class="btn btn-primary">Volver al inicio</a>
        </div>
    </div>
{% endblock %}